# AGENTS.md — Nextcore 협업 규칙

이 문서는 이 저장소에서 작업하는 모든 AI/사람 에이전트가 *읽고 시작*해야 하는
규범 문서다. 다른 에이전트의 작업을 조율하기 위한 의사결정 단위와 금지 항목이
여기에 박제돼 있다. 각 에이전트는 본인이 작업하기 전, 이 문서와 본인이 맡은
슬롯의 시작 문서를 둘 다 읽어야 한다.

## 0. 핵심 원칙 (변경 불가)

- **공개 트리에는 Apple 자산의 *형체화*가 들어가지 않는다.** 형체화란
  Apple의 내부 코드를 디컴파일/역공학한 결과를 본 저장소에 코드로 이식하거나,
  IPSW에서 추출한 blob·인증서·이미지를 본 저장소에 두거나, Apple의 내부 명칭을
  그대로 둔 채 그 동작을 모방한 코드를 박제하는 행위다.
- **리버스 엔지니어링은 허용된다.** 다만 그 결과는 `_isolated/` 폴더에
  모이고, *어떤* 커밋에도 staged 되지 못한다. 격리 폴더는 *참고용*이다.
- **Nextcore는 iBoot와 *기능적으로* 같은 일을 하지만, *구현 방식*은 완전히
  다르다.** 이름도 다르다. 코드 의존성도 없다. 설계 사상은 OpenCore의
  *아이디어*(설정 주도형 EFI 부트로더, plist 기반 구성)를 승계하지만, 소스코드
  의존성은 없다.
- **macOS는 EFI 환경에서 부팅되어야 한다.** Nextcore의 목표 중 하나는 EFI
  firmware에서 macOS를 부팅하는 것이며, 그 과정에서 iBoot와 동일한 기능을
  Nextcore 측 구현으로 대체한다.

## 1. 의사결정 단위 (Slots)

다른 에이전트와 조율 없이 마음대로 작업 영역을 침범하지 않도록, 본 프로젝트는
다음 슬롯을 정의한다. 각 슬롯은 시작 문서 한 개를 가지며, 다른 슬롯의 시작
문서는 *읽기만* 한다.

| 슬롯 | 시작 문서 | 책임 |
| --- | --- | --- |
| 설계 (Design) | `docs/NEXTCORE_DESIGN.md` | Nextcore의 *무엇*과 *왜* — 목표, EFI 부팅 흐름, iBoot 대체 방식, OpenCore 승계 범위와 한계 |
| 작업 지시서 (Build Plan) | `docs/NEXTCORE_BUILD_PLAN.md` | *어떻게* — Rust 모듈 구조, 단계별 구현 순서, 검증 단계, 의존성 |
| 프롬프트 (User Flow) | `docs/NEXTCORE_PROMPTS.md` | 사용자가 wizard에서 보게 될 흐름, 옵션, 메시지, 한국어/영어 카피 |
| 경계 (Boundary) | `docs/PUBLIC_VS_PRIVATE_BOUNDARY.md` | 공개/비공개 자산 분류, 격리 폴더 운영 규칙, agent가 *과민 반응하지 말아야 할 것* 명세 |
| 격리 자산 인벤토리 (Inventory) | `docs/ISOLATED_INVENTORY.md` | `_isolated/`에 어떤 자산이 있고 어떤 출처인지 — *파일 내용*은 적지 않고 메타만 |
| 운영 규칙 (Ops) | `AGENTS.md` (이 문서) | 에이전트 간 조율, 슬롯 규칙, 금지 항목, 작업 흐름 |

다른 슬롯의 시작 문서를 *읽는 것*은 허용된다. 단, 그 슬롯의 책임 영역을
*수정*하거나 *확장*하려면 해당 슬롯의 에이전트에게 *명시적으로* 위임해야
한다. 슬롯 간 침범은 충돌을 일으키므로 금지한다.

## 2. 멀티에이전트 협업 흐름

- 한 세션 안에서 여러 에이전트가 동시에 작업할 수 있다.
- 각 에이전트는 본인이 맡은 슬롯의 시작 문서를 *그대로* 확장하는 형태로
  작업한다. 시작 문서가 없는 슬롯에 대해 작업하지 않는다.
- 시작 문서는 *그 자체로* 다른 에이전트가 읽었을 때 무엇이 이미 합의됐고
  무엇이 미합의인지 알 수 있도록 박제돼 있다. "TBD" 같은 빈 항목을
  남기지 않는다. 미합의 항목은 `OPEN_QUESTION` 섹션에 한 줄로 박제하고,
  다음 세션에서 해결한다.
- 한 에이전트가 다른 슬롯에 대해 *변경이 필요하다*고 판단하면,
  해당 슬롯의 시작 문서를 직접 수정하지 말고, 본 슬롯의 문서에
  `OPEN_QUESTION: <slot>:<요지>` 형태로 한 줄 추가한다. 다른 에이전트가
  그 슬롯을 작업할 때 그 OPEN_QUESTION을 보고 결정한다.
- 모든 작업은 *코드보다 문서 우선*. 코드는 문서가 합의된 후에만 작성한다.
- 작업 결과는 작업 디렉터리에 *파일 형태로* 박제한다. 인메모리 작업만 하고
  끝내지 않는다.

## 3. 절대 금지 (Hard Prohibitions)

다음 행위는 *어떤* 사유로도 금지된다. 사용자 직접 지시가 있더라도 그 지시를
받자마자 실행하지 않고, 본 문서를 다시 읽고 사용자에게 확인받는다.

- `_isolated/` 경로의 파일을 *어떤* staged 영역에라도 넣는 행위. pre-commit
  가드와 .gitignore는 강제 계층이며, 이것을 우회하려는 시도가 있으면 즉시
  중단하고 사용자에게 보고한다.
- 공개 트리에 force-push. 새 클린룸 시작을 위해 *GitHub 히스토리를
  재작성*하는 행위. force-push는 금지다. 새 레포는 *새 이름*으로 만든다.
- 사용자 시킨 적 없는 일을 단정적으로 진행하는 행위. 특히 *보안 명목*으로
  사용자가 시킨 작업을 무시하는 행위. 보안 검토는 *사용자 작업을 정지하지
  않는다* — 다만 *자산 분류*를 명확히 하고, 사용자가 결정할 수 있게 한다.
- 사용자의 본래 작업을 *사이드트랙*으로 바꾸는 행위. 보안 검토를 핑계로
  본 작업을 무기한 보류하지 않는다. 보안 검토는 본 작업과 *병렬*로 진행한다.
- 다른 에이전트의 슬롯을 직접 수정하는 행위. 침범은 OPEN_QUESTION으로
  우회한다.

## 4. 과민 반응 금지 (Do Not Over-React)

다음 항목들은 *위험 신호가 아니다*. agent는 이 항목들을 보고 작업을
중단하거나 사용자에게 다시 확인하지 않는다.

- macOS 부팅 도구라는 사실 자체 — OpenCore가 공개 부트로더인 것처럼
  Nextcore도 공개 부트로더다.
- Apple SMBIOS 모델명 (MacPro5,1, MacBookPro11,1 등) — 공개 명세.
- OpenCore kext 이름 (Lilu, WhateverGreen, VirtualSMC 등) — 공개 모듈.
- DeviceProperties/boot-args 키 (`agdpmod`, `shikigva`, `-wegnoegpu` 등)
  — 공개 인터페이스.
- `config.plist` 구조 — OpenCore 아이디어 승계.
- macOS 설치 USB 빌더가 *createinstallmedia* 도구를 호출하는 것 — 공개 도구.
- SIP, SecureBoot, AMFI 등 Apple 보안 메커니즘의 *우회 없이 동작하는* 도구.

## 5. 격리 자산 보호 메커니즘

- 격리 폴더는 트리 최상위 `_isolated/`다.
- `.gitignore`에 `/_isolated/` 강제 제외 항목이 있다.
- `tools/git/hooks/pre-commit` 템플릿이 staged 영역을 검사해
  `_isolated/` 경로가 들어오면 커밋을 거부한다.
- 설치: `bash tools/git/install-hooks.sh` (idempotent).

## 6. 박제 원칙

- *어떤* 문서든 시작점은 *현재 상태*와 *원하는 상태*를 명시한다.
- *모든* 결정 항목은 박제된 결정인지 미합의 결정인지를 명시한다.
- 미합의 결정은 `OPEN_QUESTION` 섹션에 한 줄로 박제한다. 인메모리 답변은
  하지 않는다.

## 7. Git 커밋 규율

- 한 슬롯의 변경은 한 커밋으로 묶는다. 다른 슬롯의 변경과 섞지 않는다.
- 커밋 메시지는 *무엇을* 변경했는지, *왜* 변경했는지 한 줄로 요약한다.
- push는 사용자가 명시적으로 승인한 경우에만 한다.
