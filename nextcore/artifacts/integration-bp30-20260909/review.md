# BP30 independent publication review

Reviewed parent ea0a62e8e75efd517702873535dc1dfb0ffc6b42 read-only. One confirmed finding: the tiered evidence README used a nonexistent artifact directory in its reproduction command. Root corrected the invocation and documented the publication correction; its refreshed README hash was checked. All other historical source/provenance/execution evidence stayed unchanged.

The three baseline public bundles contain 134 files including their manifests, all matched. The 150 changed ordinary parent files were hashed; no binary asset, credential or original-image coordinate leak was found. Private-reference scan matches are existing boundary documentation and exclusion guards, not new private payloads. Authored assembly and serial logs retain their own fixture origins.

The bypass regression suite (4 methods, 13 scenarios), package policy suite (15 tests, including an actual temporary Git/Cargo duplicate resolver), and clean module/inventory verifier passed. Seven modules and the ISE-owned auxiliary package resolve to the declared integrated source revisions. The new runtime runner and CI preserve strict child failures and real provider negative-control receipt validation; callback-failure variants are explicitly separate. No further concrete false-PASS or current pin/schema mismatch was found.

The public XNU references support the documented SPTM startup argument roles, build-time SPTM/PPL selection, external boot-argument ABI dependency, and runtime DT full-length handling. The Apple SoC table has the stated publication date and does not establish the target image's entry ABI. Target ABI/live-state and OS/Metal completion remain explicitly unverified. Private original-image provenance claims were outside this independent review; no original asset was opened.

No OVMF replay or production source change was performed. Final recursive integration and final exact-head CI are the parent's remaining publication checks. Exact source hashes, commands, logs, correction hashes and references are in the accompanying receipts.
