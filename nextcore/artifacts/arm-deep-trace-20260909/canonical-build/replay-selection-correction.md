The first canonical gate supplied the non-trace arm-jit binary to a control requiring actual TRACE_CONFIG_INVALID. That binary instead correctly stopped at PROVIDERS_PENDING/NOT_READY without guest execution, so the strict control failed. The four other actual runs and28 CLI controls passed. No product or predicate changes follow. The corrected control uses the separately built tiered-only combined binary, which implements the old trace parser and must reject the deep-only selector before guest entry. The initial receipt remains unchanged.
