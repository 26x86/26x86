# BP34 fresh integration evidence

A fresh recursive checkout of750bda209608cab6beca01da5bea1733150b027c passes all53
recorded validation commands. It resolves seven module repositories and eight
owned packages, including the canonical nested Rust memory service.

Workspace502 (including doctests), Python149, GUI25, reference98,
service44, Core no-default232+3doctests and Vulkan119 pass. Existing ignored
cases and Clippy warnings remain in the logs. Actual x86 EFI200 cases comprise
previous194 plus the six NXDYN dynamic controls; six CLI-only rejections remain
separate. DT28 and dynamic30 malformed-reader controls pass. Six captured Arm
transitions execute through the current native C/Rust service and all13 comparator
regressions pass. Actual debug NXAPFS code generation/link remains a required gate.

Fresh clone transport uses committed local repositories and temporary URL maps;
this does not itself establish network publication. Final parent publication is
verified separately by a canonical GitHub recursive clone and exact-head CI.
Independent module clones resolve canonical published Git dependencies without
root workspace patches. Their original compact receipts and command logs are
preserved under canonical-modules. Historical native/source proof keeps its own
source identity. The original authored EFI omission negative and separate Arm
oracle/comparison bundles remain unchanged.

Raw reports, serials, commands and selected authored source/data files are copied
byte-for-byte. EFI binaries, generated build trees, firmware variable disks and
ESP disk images are omitted; binary hashes remain in their reports. Historical
absolute paths describe the recorded run. Reproduction of the active firmware
gates uses the module tools and commands in the wiki; the recorded local driver
requires editing its source/output paths and is not a portable command as-is.

No original Apple input is used in this suite. These authored observations do not
establish normal macOS boot, complete SPTM services, physical GPU portability or
guest Metal. The historical BP33 debug compiler error and QEMU shutdown failure
remain in their separate unchanged evidence bundle.
